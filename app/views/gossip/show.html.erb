<div class="jumbotron" style="background-color:#FEF4F7">
  <h1 class="display-5" style="color:#7146B2">🔥 Bienvenue sur la page du potin numéro <%= @gossip.id %> 🔥</h1>
  <p class="text-center" style="color:#7146B2">- Voici ci-dessous le potin en détail -</p>
  <hr class="my-4">
  <div class="card text-center">
  <h4 class="card-header"><%= @gossip.title %></h4>
  <div class="card-body">
    <p class="card-title"><%= @gossip.content %></p>
    <h6 class="card-text">- <%= @gossip.user.last_name %> <%= @gossip.user.first_name %> -</h6><p/>
    <a href="<%= user_path(@gossip.user_id) %>" class="btn" style="background-color:#7146B2; color:#FFFFFF">Voir la fiche de l'auteur</a>
    <a href="<%= gossip_index_path %>" class="btn btn-outline-secondary">Revenir à la liste des potins</a>
  </div>
  <div class="card-footer text-muted">
    (Crée le <%= @gossip.created_at %>)
  </div>
</div>
</div>
