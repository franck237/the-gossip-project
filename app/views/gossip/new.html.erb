<div class="jumbotron" style="background-color:#FEF4F7">
  <h1 class="display-5" style="color:#7146B2">🔥 Bienvenue sur la page de création d'un potin 🔥</h1>
  <p class="text-center" style="color:#7146B2">- Remplis le formulaire ci dessous -</p>
  <hr class="my-4">

<div class='container mt-4'>
<div class='row justify-content-center'>
      <div class='col-6'>
            <% if @gossip != nil %>
            <div class ='alert alert-danger'>
                  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
                  <ul>
                  <% @gossip.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                  <% end %>
                  </ul>
            </div>
            <% end %>

<%= form_tag url_for(action: 'create'), method: "post" do %>

      <%= label_tag 'Titre : ' %>
      <%= text_field_tag 'title' %>
      <br /> <br />

      <%= label_tag 'Contenu :' %>
      <%= text_area_tag 'content' %>
      <br /> <br />

      <%= submit_tag "Create Gossip" %>

    <% end %>

