<div class="jumbotron" style="background-color:#FEF4F7">
  <h1 class="display-5" style="color:#7146B2">🔥 Bienvenue sur la page de l'auteur numéro <%= @user.id %> 🔥</h1>
  <p class="text-center" style="color:#7146B2">- Voici ci-dessous la description de cet auteur -</p>
  <hr class="my-4">
  <p class="text-center">Prénom : <%= @user.first_name %></p>
  <p class="text-center">Nom : <%= @user.last_name %></p>
  <p class="text-center">Description : <%= @user.description %>
  <p class="text-center">Email : <button type="button" class="btn btn-link"> <%= @user.email %></button></p>
  <p class="text-center">Age : <%= @user.age %></p>
  <p class="text-center">
  Ville : <a class="btn btn-link btn-lg" href="<%= city_path(@user.city_id) %>" role="button"> <%= @user.city.name %> </a></p>
  <br/>
  <p class="text-center">Liste des Gossips de l'auteur : </p>

  <%= @user.gossips.each do |gossip| %>

  <div class="list-group">
  <a href="<%= gossip_path(gossip.id) %>" class="list-group-item list-group-item-action" style="color:#7146B2">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1"><%= gossip.id %> - <%= gossip.title %></h5>
      <small><%= gossip.created_at %></small>
    </div>
    <p class="mb-1"><%= gossip.content %></p>
  </a>
  </div>
  <br/>
  <% end %>

  
</div>
